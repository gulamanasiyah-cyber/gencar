# PLAN — Sabtu, 6 September 2026

## Ringkasan Kerja Hari Ini (Jumat, 5 Sep 2026)

Semua perubahan di-push ke branch `gencar-v2` (cc1063c..af34544). Tema besar: **sistem absensi cerdas + UI member & admin**.

### 1. Absensi otomatis (scan QR / auto-detect)
- **`543c744`** Auto-detect kegiatan eligible saat scan QR + resolusi konflik + snapshot wilayah (`absensi.desa_id/kelompok_id/catatan`).
- **`acdc2f9`** Kegiatan tanpa info jam dianggap seharian penuh (agar bisa discan).
- **`0e75fe8`** Status `gps_required` spesifik saat event terhalang GPS (no_gps / out_of_range).
- **`993f4c0`** Fix stale closure GPS di callback scanner (pakai ref).
- **`20d9aac`** **Scheduler cron 15 menit** pencatatan `alpha` otomatis utk kegiatan selesai (`server/src/jobs/alphaSweep.ts`, `wrangler.toml` crons, endpoint `/_internal/alpha-sweep`).
- **`33a9e95`** **Ajukan Izin** member utk kegiatan mendatang (`izinSumber="ajuan"`) + admin batalkan (hapus) via tab Pengajuan→Izin.

### 2. UI Member
- **`de16668`** Riwayat absensi di beranda via `GET /api/absensi/mine`.
- **`13a67b5`** Riwayat 5 terbaru + modal semua riwayat dengan filter status.
- **`29ae260`** Card "Ajukan Izin Kegiatan" mandiri.
- **`0b6a2c3`**, **`4966292`**, **`88f8119`** Perbaikan modal lokasi (format waktu masuk akal, km, hapus popup ganda), penempatan tombol close.
- **`1c0d2e7`** Komponen `Select` dipindah ke `components/Select.tsx` (dipakai bersama).

### 3. UI Admin
- **`a9b1a3e`** Halaman baru **QR Wilayah** (kartu QR per daerah/desa/kelompok + unduh).
- **`50a1e53`**, **`7bc4f6d`** Aturan **peserta wajib**: auto-aktif desa/kelompok pembuat + mutual-exclusion desa/kelompok/anggota.
- **`7b339c3`**, **`a059a21`**, **`af34544`** Panel ajuan izin: filter pencarian + status, filter lengkap per kegiatan/desa/kelompok dalam **modal**, pakai komponen `Select` bersama.

### 4. Lain-lain
- **`c0865d9`** Cooldown hobi 30 hari hanya jika nilai benar-benar berubah (ganti avatar aman).

---

## Rencana Kerja Besok

### A. Verifikasi & stabilisasi fitur besar (prioritas tinggi)
- [ ] **Uji menyeluruh scheduler alpha** (cron tiap 15 mnt):
  - Pastikan deploy ke remote menjalankan cron (bukan hanya lokal).
  - Konfirmasi kegiatan historis ikut diproses, idempoten (tidak dobel), skip yang sudah hadir/izin.
  - Cek kinerja batch kalau banyak kegiatan (potensi timeout 30s cron) → pertimbangkan LIMIT per run.
- [ ] **Uji lintas perangkat flow absen member**: scan QR → success / multiple / gps_required / no_event semua tampil via modal. Pastikan di HP real (kamera + GPS).
- [ ] Cek `POST /api/_internal/alpha-sweep` dilindungi (saat ini publik) — beri proteksi (secret header / role admin).

### B. Menyelesaikan catatan yang belum tuntas
- [ ] `pesertaFilters` (usia/gender/pendidikan) saat ini **tidak disimpan backend** saat buat kegiatan → putuskan & simpan agar filter peserta wajib & alpha akurat. (Sudah jadi temuan di sesi ini.)
- [ ] Konfirmasi perilaku "wajib hadir" scheduler sudah sesuai aturan wilayah (desa/kelompok/daerah, undangan lintas wilayah).

### C. Potensi lanjutan / backlog
- [ ] Dashboard/statistik menampilkan breakdown `alpha` per kegiatan setelah sweep berjalan.
- [ ] Notifikasi ke member saat tercatat alpha (WhatsApp/push) — bila diminta.
- [ ] Perbaikan tampilan modal map pada layar sempit & accessibility (label/tab order).

### D. Deployment
- [ ] `npm run build` + `wrangler deploy` → pastikan cron aktif & assets SPA benar.
- [ ] Jalankan migrasi D1 remote (`wrangler d1 migrations apply gencar-db --remote`).
