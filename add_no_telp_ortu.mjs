import { createClient } from '@libsql/client'; import * as dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); const db = createClient({ url: process.env.TURSO_DATABASE_URL, authToken: process.env.TURSO_AUTH_TOKEN, }); async function main() { try { await db.execute('ALTER TABLE generus ADD COLUMN no_telp_ortu TEXT;'); console.log('success'); } catch (e) { console.error(e); } } main();  
