# User Taste Preferences
- Prefers Indonesian (Bahasa Indonesia) for technical discussions. Confidence: 0.9
- Prefers discussion-style responses ("diskus aja") over direct prescriptive answers. Confidence: 0.85
- Wants practical investigation and testing, not just theoretical suggestions. Confidence: 0.85
- Uses `scratch/` directory for experimental/debugging scripts and iteration. Confidence: 0.8
- Runs code on VPS for production-like testing and checks locally for comparison. Confidence: 0.8
- Iterates rapidly when debugging — creates and re-runs scripts multiple times to narrow down issues. Confidence: 0.8
- TypeScript-first workflow using `npx tsx` for quick script execution. Confidence: 0.9
- Next.js application structure (route.ts, schema.ts patterns). Confidence: 0.85
- Works with Meta/Firebase ecosystem (WhatsApp Business API, Firebase Cloud Messaging). Confidence: 0.85
- Prefers checking configuration and console settings systematically when something fails. Confidence: 0.8
- Provides raw context (logs, errors, what was tried) rather than summarizing issues abstractly. Confidence: 0.8
- Expects the assistant to be aware of dev server auto-refresh (Vite HMR) and not waste time on manual build verification. Confidence: 0.85
- Wants auto-reload for both frontend and backend during development — explicitly asked to configure Wrangler auto-reload so server-side changes don't require manual restart. Confidence: 0.9
- Provides visual design references/mockups to guide UI implementation rather than describing aesthetics in text. Confidence: 0.85
- Sends raw HTML markup as layout specification with terse directives (e.g. "penyusunan perbaiki") — expects the assistant to interpret the structure, improve it, and return a cleaner version. Confidence: 0.85
- Issues bare-bones imperative feature requests in Indonesian with no detail or spec (e.g. "halaman member tambah search dan export excel") — expects the assistant to explore the codebase, plan, and implement autonomously without asking clarifying questions. Confidence: 0.7
- Iterates on UI by sending screenshots with brief critiques (e.g. "masih terlalu beda dari rujukan") — expects close visual fidelity to provided references. Confidence: 0.85
- Prefers full-width page layouts without rounded border containers on the outer page shell — content should fill the whole viewport. Confidence: 0.85
- Prefers floating card-style sidebar with border-radius on all screen sizes (not just mobile), with logo/brand and nav items integrated inside. Confidence: 0.9
- Prefers no topbar/header at all — wants the topbar removed entirely and the logo/brand integrated inside the floating sidebar instead. Confidence: 0.85
- Prefers viewport-locked height layout (100dvh on parent with overflow: hidden) so sidebar and main content scroll independently within fixed containers, rather than the page itself scrolling. Confidence: 0.85
- Prefers custom-built UI components (e.g. custom `<Select>`) over native browser form controls (e.g. `<select>`) for consistent styling and behavior. Confidence: 0.9
- Mobile layout should show only cards, never tables — explicitly hides table view and view-toggle on small screens. Confidence: 0.9
- Prefers generous breathing space in card components (larger padding and internal gaps, e.g. 20px padding, 12px gap between elements) — rejects cramped card layouts. Confidence: 0.85
- Wants data cards with clear visual hierarchy (header section → meta/tags → separated info rows with label-value pairs → actions) rather than flat concatenated info lines joined by separators. Confidence: 0.85
- Prefers semantic CSS class names (e.g. `.member-card-head`, `.member-card-actions`) over inline styles for component structure. Confidence: 0.8
- Maintains plan/spec documents (e.g. `.claude/plans/`) and expects the assistant to find, read, and implement features based on them when referenced — gives brief directives like "berdasarkan plan" and expects the assistant to self-discover the relevant spec. Confidence: 0.85
- Asks the assistant to build new UI components according to the existing design system ("sesuai dengan design system yang ada") — expects reuse of established design tokens and component patterns rather than introducing new styling approaches or libraries. Confidence: 0.8
- Prefers icon/controls buttons to hug their content (`width: auto`, not stretched full-width) while keeping padding, rather than expanding to fill container width. Confidence: 0.85
- Wants visible gaps between adjacent footer/control elements (e.g. between a button and nearby text) — elements should never sit flush/touching each other, even when using `justify-content: space-between`. Confidence: 0.85
- Prefers concise, context-sensitive UI text over verbose static descriptions that list all possibilities at once — considers text "pile-up" as ugly and wants only the relevant information for the current user's context. Confidence: 0.85
- Prefers filter controls hidden behind a toggle button (that opens a modal/panel on click) rather than always-visible inline filter panels — progressive disclosure pattern for secondary UI controls. Explicitly rejected inline dropdown selects next to search input. Confidence: 0.9
- Uses WezTerm as their terminal emulator. Confidence: 0.9
- Wants dropdown/select menus to overflow and not be clipped by parent container overflow — dropdowns must escape scrolling/clipping parents (e.g. modals with `overflow: auto`) to remain fully visible. Confidence: 0.9
- Wants smart adaptive dropdown positioning — dropdowns should detect viewport boundaries and auto-flip direction (e.g. open upward when near the bottom edge) rather than always opening in a fixed direction. Confidence: 0.9
- Prefers custom modal components over native browser dialog boxes (alert, prompt, confirm) for all user-facing messages, link displays, and confirmations — considers browser alerts ugly and unprofessional. Confidence: 0.9
- Prefers mobile-native sharing via deep links (e.g. `wa.me`, `t.me/share`) and Web Share API over web-only sharing — wants features to trigger the phone's native app ecosystem for sharing content to specific apps. Confidence: 0.9
- Wants social share buttons to use brand-specific colors and inline SVG icons (e.g. WhatsApp green #25D366, Instagram gradient) rather than generic/neutral button styling — share buttons should visually match the identity of the platform they link to. Confidence: 0.85
- Prefers minimal UI without redundant controls — flags any element as "redundant" if its functionality is already accessible elsewhere (e.g. a "Tutup" close button when the modal already has an X, or a "Buat QR" button inside a detail modal when QR can already be triggered from the card/table). Confidence: 0.85
- Wants real database data in production, not hardcoded mock/placeholder data — expects the assistant to identify and remove any fallback mock data so the UI always reflects actual DB state. Confidence: 0.85
- Expects surgical precision in scope — when requesting a change to a specific element, only that element should be modified, not its parent container or surrounding siblings. Corrects over-implementation aggressively (e.g. hiding one dropdown vs. hiding the entire filter bar). Confidence: 0.85
- Approves plan proposals by typing just the option number (e.g. "1.") — expects the assistant to present numbered options and interpret a bare number as full approval to proceed. Confidence: 0.9
- Prefers horizontal inline layout for card metadata (time & location side-by-side with icons, no label text) over vertical stacked rows with label-value pairs — considers the horizontal icon+value pattern cleaner for compact cards. Confidence: 0.8
- Prefers database migration (preserving existing data) over destructive approaches like deleting and recreating databases — pushes back when assistant suggests wiping local DB. Confidence: 0.85
- Wants natural browser focus behavior on interactive elements — when toggling checkboxes in a list, focus should stay on the toggled checkbox rather than being programmatically forced back to another element (e.g. search input). Corrected assistant twice to remove forced focus behavior. Confidence: 0.85
- Prefers searchable list/checkbox UI over chip/tag selectors when the number of selectable items is large — switches to list pattern proactively when item count is high ("kelompok pake ui list juga soalnya kelompoknya banyak"). Confidence: 0.9
- Expects forms to auto-deduce field values from user context (e.g. role-based defaults for "tingkat" field) rather than leaving them empty/blank — smart defaults should be applied automatically when opening a creation form. Confidence: 0.85
- Prefers removing form fields entirely when their value can be fully determined from user context — rather than auto-filling a selectable field, eliminate the UI control completely (e.g. removed "tingkat" select because it's always derivable from role). Confidence: 0.9
- Prefers toggle-based mode switching for selection scopes — e.g. a toggle that switches between "all members" (no selection UI) and "select specific" (shows picker UI), rather than having a separate "optional" checkbox or always showing the selection panel. Confidence: 0.85
- Prefers textarea and longer-input fields on their own row, separate from compact fields (date, time) in form grids — does not want multi-line inputs squeezed into a grid column alongside single-line inputs. Confidence: 0.85
- Wants ImageUploadInput and related caption fields stacked vertically (full-width, one below the other), not side-by-side in a form grid — image upload boxes are wide interactive elements that should not be squeezed into half-width grid columns. Confidence: 0.85
- Wants typed-confirmation pattern for destructive actions — instead of a simple confirm dialog, requires the user to type a specific phrase (e.g. "hapus {item name}") to enable the delete button, with clear instructions and code-styled hint in the modal. Confidence: 0.9
- Prefers lowercase text for confirmation prompts and UI instructions — explicitly rejected capitalized confirmation text ("Hapus") as confusing, wanted lowercase ("hapus") instead. Note: later overrode this in favor of cross-component consistency when matching an existing modal pattern. Confidence: 0.7
- Expects strict visual consistency across similar UI components — all delete confirmation modals, for example, must use the same template (warning box, confirmation text style, button layout, error display). Will call out any inconsistency between parallel patterns. Confidence: 0.9
- Wants long text values in card metadata (e.g. addresses/lokasi) truncated with CSS ellipsis (`text-overflow: ellipsis`) rather than wrapping or overflowing — prefers `max-width` + `overflow: hidden` + `white-space: nowrap` pattern for compact card display. Confidence: 0.85
- Prefers open access in UI pickers for all roles (no role-based hiding of desa/kelompok selectors) — authorization and approval logic belongs server-side, not in the frontend UI. All users should see all options because they can request cross-role collaboration. Confidence: 0.9
- Wants role-based visibility for action buttons (edit/delete) — admin_daerah sees all, admin_desa sees all within their desa + daerah-level activities, admin_kelompok sees all activities within their own kelompok + daerah-level activities. Invited participants must NOT see these buttons. UI should hide actions the user is not authorized to perform. Confidence: 0.9
- Wants detail/view modals to mirror the edit form layout in read-only mode — same structure, labels, and grid arrangement but with plain text instead of input fields. Expects consistency between edit and detail views so the detail view looks like a "locked" version of the form. Confidence: 0.85
- Prefers visual/interactive representations for geographic data — wants latitude/longitude coordinates displayed as an embedded map view (e.g. OpenStreetMap iframe) rather than as raw numeric text. Confidence: 0.85
- Prefers hidden scrollbars in modals/scrollable containers — uses CSS (`scrollbar-width: none`, `-ms-overflow-style: none`, `::-webkit-scrollbar { display: none }`) to hide scrollbars while keeping scroll functionality. Considers visible scrollbars ugly. Confidence: 0.85
- Wants all card types within the same gallery/grid view to share a unified visual style (e.g. polaroid caption format) — rejects cards that break the pattern (e.g. quote cards styled as dark blocks when photo cards use light captions). Confidence: 0.85
- Prefers reverting to original/working state over continuing to patch a broken change — when a UI modification causes visual regressions ("makin rusak"), wants the assistant to roll back to the prior working version rather than keep iterating forward on a broken approach. Functional correctness (text visible, layout intact) takes priority over aesthetic improvements. Confidence: 0.8
- Expects seamless post-action transitions in authentication flows — after completing a password setup via magic link, the user should be auto-logged in and redirected directly to the member dashboard, never left stranded on a "success" page requiring manual re-login. Every terminal action in a flow should land the user at their next logical destination. Confidence: 0.85
